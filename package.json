{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "dotnet fable watch src --runFast vite --open",
    "build": "dotnet fable src --run vite build"
  },
  "devDependencies": {
    "@web/test-runner": "^0.13.23",
    "@web/test-runner-commands": "^0.6.0",
    "vite": "^2.7.9"
  },
  "version": "1.0.0",
  "dependencies": {
    "lit": "^2.0.2"
  }
}